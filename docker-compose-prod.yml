name: eco-ecotrack-dashboard
services:
  eco-ecotrack-dashboard:
    image: eco-ecotrack-dashboard
    container_name: eco-ecotrack-dashboard
    build:
      context: .
      dockerfile: Dockerfile.prod
    volumes:
      - './eco-ecotrack-dashboard:/usr/src/app'
    ports:
      - 127.0.0.1:4200:80
    networks:
      - jacnet

networks:
  jacnet:
    external: true
    name: jacnet
