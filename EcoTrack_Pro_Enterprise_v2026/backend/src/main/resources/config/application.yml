resilience4j:
  retry:
    instances:
      geminiRetry:
        max-attempts: 3
        wait-duration: 2s
        enable-exponential-backoff: true
  circuitbreaker:
    instances:
      geminiService:
        sliding-window-size: 10
        failure-rate-threshold: 50
